# cli

TSDHN command-line client.

Talks to the orchestrator service over HTTP. See the workspace root
[README](../../README.md) for full setup instructions.

## Entry point

```sh
uv run tsdhn            # invoke the CLI
uv run tsdhn --dev      # dev mode
```
