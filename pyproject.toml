[project]
name = "picv-2025"
version = "0.0.1"
description = "TSDHN tsunami simulation workspace"
requires-python = ">=3.14"
readme = "readme.md"
license = { text = "MIT" }
authors = [
    { name = "David Duran", email = "dadch1404@gmail.com" },
]

[tool.uv.workspace]
members = ["packages/tsdhn", "packages/api", "packages/tsdhn-parity"]

[tool.uv.sources]
tsdhn = { workspace = true }
tsdhn-api = { workspace = true }
tsdhn-parity = { workspace = true }

[dependency-groups]
dev = [
    "ruff==0.15.20",
    "mypy==2.2.0",
    "types-PyYAML==6.0.12.20260518",
    "pytest==9.1.1",
    "pytest-asyncio==1.4.0",
    "pytest-xdist==3.8.0",
    "pytest-cov==7.1.0",
    "httpx2==2.5.0",
    "coverage==7.15.0",
    "diff-cover==10.5.1",
    "bandit==1.9.4",
    "pip-audit==2.10.1",
]
build = [
    "uv_build==0.11.28",
]

[tool.ruff]
target-version = "py314"
line-length = 88
indent-width = 4
extend-exclude = ["__pycache__", "dist", "build"]

[tool.ruff.lint]
# E/F/W = pycodestyle/pyflakes/pycodestyle warnings
# I = isort
# B = flake8-bugbear
# UP = pyupgrade
# S = flake8-bandit (Python security SAST)
# SIM = flake8-simplify
# RUF = ruff-specific
select = ["E", "F", "W", "I", "B", "UP", "S", "SIM", "RUF"]

[tool.ruff.lint.isort]
known-first-party = ["tsdhn", "api", "tsdhn_parity"]

[tool.ruff.lint.per-file-ignores]
"**/tests/**/*.py" = ["S101", "S105", "S106"]  # asserts and dummy tokens are fine in tests
# S603 is suppressed: every subprocess call uses arguments from
# validated floats, local Path objects, or frozen dataclass fields.
"packages/tsdhn-parity/tsdhn_parity/adapters/fortran.py" = ["S603"]
"packages/tsdhn/tsdhn/render/ttt_inverso.py" = ["S603"]
"packages/tsdhn/tests/test_pipeline_golden.py" = ["S603"]
"scripts/capture_matlab_fixtures.py" = ["S603"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"

[tool.mypy]
python_version = "3.14"
strict = true
ignore_missing_imports = true
warn_unused_ignores = true
disallow_untyped_defs = true

[tool.coverage.run]
branch = true
# tsdhn-parity is a comparison harness against legacy MATLAB/Fortran
# reference outputs, not shipped product code (see its readme). Whether
# the harness itself is well-tested is a different question from whether
# the product is, so it is not measured here.
source = ["packages/tsdhn/tsdhn", "packages/api/api"]
omit = ["**/tests/*", "**/__init__.py"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]

[tool.pytest.ini_options]
testpaths = ["packages/tsdhn/tests", "packages/api/tests", "packages/tsdhn-parity/tests"]
pythonpath = ["."]
addopts = "-ra"
asyncio_default_fixture_loop_scope = "function"
markers = [
    "golden: requires the real compiled toolchain; see mise run test-golden",
    "integration: uses disposable databases on the mise-managed PostgreSQL cluster; see mise run test-integration",
]
