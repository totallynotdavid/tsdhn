# Generate with: openssl rand -hex 32
COMPUTE_API_TOKEN=

# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=

ORIGIN=http://localhost:3000

# Leave empty unless a browser calls the API directly.
ALLOWED_ORIGINS=

# Passwordless database endpoint used to build runtime-role DSNs. The Docker
# Compose file supplies its postgres-service default; override for other hosts.
COMPUTE_RUNTIME_DATABASE_URL=postgresql://postgres:5432/tsdhn

# Password for the runtime web role. Generate with: openssl rand -hex 32
APP_DB_PASSWORD=

# Passwords for the least-privilege queue roles the API, worker, and retention
# pass connect as. Generate each with: openssl rand -hex 32
COMPUTE_PRODUCER_PASSWORD=
COMPUTE_WORKER_PASSWORD=
COMPUTE_PURGER_PASSWORD=

# Browser-reachable MinIO endpoint for output download URLs.
MINIO_PUBLIC_ENDPOINT=localhost:9000

# Task queue. The queue name and the schema rqueue owns; both reach the
# migration step and every process that reads or writes the queue, so a value
# set here applies everywhere or nowhere.
COMPUTE_QUEUE=simulations
COMPUTE_QUEUE_SCHEMA=task_queue
